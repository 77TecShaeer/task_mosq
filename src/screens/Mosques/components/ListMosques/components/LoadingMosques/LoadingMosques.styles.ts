import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    padding: 10,
    backgroundColor: 'white',
    // borderWidth: 1,
    // borderColor: 'gray',
    borderRadius: 8,
    marginBottom: 10,
  },
  itemContainer: {
    marginBottom: 20,
  },
});
